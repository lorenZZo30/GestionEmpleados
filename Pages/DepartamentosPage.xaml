<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:GestionEmpleadosMaui.Models"
             xmlns:vm="clr-namespace:GestionEmpleadosMaui.PageModels"
             x:DataType="vm:DepartamentosPageModel"
             x:Class="GestionEmpleadosMaui.Pages.DepartamentosPage"
             Title="Departamentos">
    <CollectionView ItemsSource="{Binding Departamentos}"
                    SelectionMode="Single" 
                    SelectedItem="{Binding DepartamentoSeleccionado}">
        <CollectionView.ItemsLayout>
            <GridItemsLayout Orientation="Vertical" Span="3" HorizontalItemSpacing="10" VerticalItemSpacing="10" />
        </CollectionView.ItemsLayout>


        <!-- Aparece si la colección está vacía -->
        
        <CollectionView.EmptyView>
            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Margin="0,50">
                <Label Text="No hay departamentos registrados." TextColor="Gray" FontSize="18" HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </CollectionView.EmptyView>

        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:Departamento">
                <Frame BackgroundColor="#1B263B" BorderColor="Transparent" Padding="15" HasShadow="True">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="{Binding Nombre}" FontSize="18" FontAttributes="Bold" TextColor="White"/>
                        <Label Text="{Binding Id, StringFormat='Código: {0}'}" FontSize="12" TextColor="#D1D1D1"/>
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>